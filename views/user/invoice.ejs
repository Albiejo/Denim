<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
    }

    .invoice-container {
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      border: 1px solid #ddd;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .invoice-header {
      text-align: center;
      margin-bottom: 20px;
    }

    .invoice-header h1 {
      color: #333;
      margin: 0;
    }

    .invoice-details {
      display: flex;
      justify-content: space-between;
    }

    .invoice-items {
      margin-top: 20px;
    }

    .item {
      display: flex;
      justify-content: space-between;
      border-bottom: 1px solid #ddd;
      padding: 8px 0;
    }

    .item:last-child {
      border-bottom: none;
    }

    .item-name {
      flex: 2;
    }

    .item-quantity,
    .item-price,
    .item-total {
      flex: 1;
      text-align: right;
    }

    .invoice-total {
      margin-top: 20px;
      text-align: right;
      font-size: 18px;
      font-weight: bold;
    }

    .invoice-footer {
      margin-top: 20px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="invoice-container">
    <div class="invoice-header">
        <h1 style="color: gray;font-weight: bolder;font-style: italic;">Denim Stores</h1><br><br>
      <h1>Invoice</h1>
    </div>

    <div class="invoice-details">
      <div>
        <p><strong>Order ID:</strong><%=Orderr.orderId%></p>
        <p><strong>Customer:<br><br>
        </strong><%=addressData.Name%></p>
        </strong><%=addressData.Housename%></p>
        </strong><%=addressData.Phone%></p>
      </div>
      <div>
        <p><strong>Order Date:</strong><%=Orderr.createdOn.toLocaleDateString()%></p>
        <p><strong>Delivery Date:</strong><%=Orderr.deliveredOn%></p>
      </div>
    </div>

    <div class="invoice-items">
        <p style="color: black;font-weight: bolder;">Items Purchased</p>
    <% for(let i=0; i< productData.length; i++){%>
      <div class="item">
        <div class="item-name"><%= productData[i].productName %></div>
        <div class="item-quantity">x<%= items[i].quantity %></div>
        <div class="item-price"><%= productData[i].offerPrice %></div>
        <div class="item-total"><%= items[i].subtotal %></div>
      </div>
    <% } %>
    </div>

    <div class="invoice-total">
      <p>Total:  <%=Orderr.totalAmount%></p>
    </div>

    <div class="invoice-footer">
      <p>Thank you for your purchase!</p>
    </div>
  </div>
</body>
</html>
