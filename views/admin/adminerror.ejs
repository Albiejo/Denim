<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 Not Found</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            background-color: #f8f9fa;
        }

        .error-container {
            text-align: center;
            max-width: 400px;
        }

        .error-image {
            max-width: 100%;
            height: auto;
        }

        .error-code {
            font-size: 3rem;
            color: #dc3545;
            margin: 10px 0 0;
        }

        .error-message {
            font-size: 1.5rem;
            color: #343a40;
            margin: 10px 0;
        }

        .back-home {
            display: inline-block;
            text-decoration: none;
            color: #fff;
            background-color: #007bff;
            padding: 10px 20px;
            font-weight: bold;
            font-size: 1.2rem;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .back-home:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <img src="https://placekitten.com/400/300" alt="404 Cat Image" class="error-image">
        <%if(typeof message !=="undefined"){%>
            <h3 style="color:red" id="errormsg"><%=message%></h3>
          <%}%>
        <p class="error-message">Oops! some issue occurred , please try again.</p>
        <a href="/admin/login" class="back-home">Go back to Admin home</a>
    </div>
</body>
</html>
